[project]
name = "predictive-maintenance-anomaly-lab"
version = "1.0.0"
description = "Safety-oriented synthetic telemetry anomaly detection lab"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
